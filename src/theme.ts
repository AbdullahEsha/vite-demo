import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#182f59",
      light: "#1e56bf",
      dark: "#5590ff",
    },
    secondary: {
      main: "#5bbc2e",
      light: "#81d55a",
      dark: "#c1ebad",
    },
  },
});
